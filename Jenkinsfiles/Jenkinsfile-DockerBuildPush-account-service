pipeline {
    agent any
    stages {
        stage('Test Git') {
            steps {
                git branch: 'main', url: 'https://github.com/mwaqee/mwfinancial.git'
                sh 'ls -la'
            }
        }
    }
}
